// Entry point: wires UI (DOM) + render (Phaser) + net (transport) around the pure sim.
// Scaffold placeholder — replaced as M1 systems land.
const app = document.getElementById('app')!
app.textContent = 'STARSHIP COMMAND — scaffold OK'
